// Usando ciclos FOR desarrolla el siguiente ejercicio

// Crea un programa que imprima en consola los números impares del 1 al 50.


var numeros =0;

for (var index=0; index <=50; index++){
    if(!(index % 2 == 0)){
    console.log(index+", ");
    }
}